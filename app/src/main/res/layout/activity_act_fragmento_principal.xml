<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Activitys.ActFragmentoPrincipal">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/mainContentRoot"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/artistlan_menu_bg">

        <!-- Glows / esferas -->
        <View
            android:id="@+id/menuGlowTop"
            android:layout_width="240dp"
            android:layout_height="240dp"
            android:alpha="0.46"
            android:background="@drawable/bg_menu_glow_purple"
            android:translationX="-40dp"
            android:translationY="-40dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:id="@+id/menuGlowCenter"
            android:layout_width="210dp"
            android:layout_height="210dp"
            android:alpha="0.26"
            android:background="@drawable/bg_menu_glow_blue"
            android:translationY="180dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:id="@+id/menuGlowBottom"
            android:layout_width="260dp"
            android:layout_height="260dp"
            android:alpha="0.34"
            android:background="@drawable/bg_menu_glow_pink"
            android:translationX="70dp"
            android:translationY="80dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <LinearLayout
            android:id="@+id/mainContent"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <include layout="@layout/layout_menu_superior" />

            <androidx.fragment.app.FragmentContainerView
                android:id="@+id/fragmentContainerView"
                android:name="androidx.navigation.fragment.NavHostFragment"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                app:defaultNavHost="true"
                app:navGraph="@navigation/navegador" />

            <include layout="@layout/layout_menu_inferior" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@drawable/bg_drawer_glass_refined"
        android:elevation="20dp"
        app:headerLayout="@layout/header_drawer"
        app:itemBackground="@drawable/bg_drawer_item_selected"
        app:itemHorizontalPadding="18dp"
        app:itemIconPadding="14dp"
        app:itemIconSize="24dp"
        app:itemIconTint="@color/drawer_item_colors"
        app:itemTextColor="@color/drawer_item_colors"
        app:menu="@menu/menu_lateral" />

</androidx.drawerlayout.widget.DrawerLayout>